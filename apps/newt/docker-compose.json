{
    "$schema": "../dynamic-compose-schema.json",
    "services": [
        {
            "name": "newt",
            "image": "fosrl/newt:1.3.1",
            "environment": {
                "PANGOLIN_ENDPOINT": "${PANGOLIN_ENDPOINT}",
                "NEWT_ID": "${NEWT_ID}",
                "NEWT_SECRET": "${NEWT_SECRET}"
            }
        }
    ]
}